import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const WorkbookScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.12 Workbook"
    blurb="Power-user data grid: 600+ players, custom columns, saved views, CSV export, optional Google Sheet / Excel import. Phase 2."
  />
);
