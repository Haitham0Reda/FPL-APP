import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const ProfileScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.13 Profile / Settings"
    blurb="Teams & drafts management, Pro status, language toggle, notification preferences, biometric login toggle, theme, data-source attribution, help/support."
  />
);
