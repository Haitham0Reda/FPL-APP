import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const PaywallScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.14 Paywall / Pro Upsell"
    blurb="Modal paywall with feature comparison. Triggered contextually: 3rd draft on Free, or Solver on Free. Never blocks first-use with a full-screen interrupt."
  />
);
