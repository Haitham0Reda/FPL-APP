import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const HomeScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.2 Home"
    blurb="Dashboard / GW hub — greeting header, deadline countdown, GW summary card, needs-attention carousel. Phase 0 milestone."
  />
);
