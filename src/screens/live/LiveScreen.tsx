import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const LiveScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.11 Live"
    blurb="Live GW tracking, mini-league standings, live rank estimate, push-notification preferences. Phase 2 (Live mini-league). Phase 0 ships the basic live ticker."
  />
);
