import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const CompareScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.8 Compare"
    blurb="Two team/draft pickers → side-by-side pitch view OR stat-grid. Differential player highlighting, value delta, projected points delta. Phase 1."
  />
);
