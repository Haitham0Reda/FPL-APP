import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const TransfersScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.7 Transfers"
    blurb="This GW + Planner (1–8 GWs timeline). Phase 1: current-GW builder with budget/hit tracking. Phase 2: multi-GW planner + transfer solver with rationale."
  />
);
