import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const StrategyScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.10 Strategy"
    blurb="Season-long notes board (masonry, taggable), chip calendar, season performance line chart (rank over time)."
  />
);
