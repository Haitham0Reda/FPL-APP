import React from "react";
import { ScreenPlaceholder } from "../_shared/ScreenPlaceholder";

export const SquadScreen: React.FC = () => (
  <ScreenPlaceholder
    prdRef="§5.3 Squad (Pitch)"
    blurb="Formation-aware pitch, player cards with captain badges, bench strip, drag-to-reorder, auto-subs simulator, tap → player detail bottom sheet. Phase 0 milestone."
  />
);
